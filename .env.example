# Template for a local .env (gitignored). Nothing in this repo reads it --
# the workflows use repo secrets of the same names. Kept for local testing
# (e.g. a manual docker push or gh API call).

GHCR_TOKEN=
ATTOGEN_READ_TOKEN=
